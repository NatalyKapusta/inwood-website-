-- ============================================================================
-- IN WOOD — виправлення: у 0015 забули GRANT для hardware_tariff_prices.
-- Без цього Postgres блокує доступ до таблиці ще до RLS-політик —
-- саме тому розділ "Фурнітура" не з'являвся в конструкторі КП.
-- Виконати цілком у Supabase Dashboard → SQL Editor (після 0016).
-- ============================================================================

grant select, insert, update on public.hardware_tariff_prices to authenticated;
