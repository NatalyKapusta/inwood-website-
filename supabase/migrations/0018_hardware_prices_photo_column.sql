-- ============================================================================
-- IN WOOD — додає колонку "photo" до hardware_tariff_prices, щоб конструктор
-- КП міг одразу показувати фото обраної фурнітури (консультанту не завжди
-- зрозуміло з самого тільки артикулу, що саме обрано).
-- Виконати цілком у Supabase Dashboard → SQL Editor (після 0017).
-- ============================================================================

alter table public.hardware_tariff_prices add column if not exists photo text;
